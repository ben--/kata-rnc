#include "rnc.h"

# suite borrow

# test cannot_borrow_i_from_ii
    char buf[6] = "II";

    rnc_borrow(buf, sizeof(buf), 'I');

    ck_assert_str_eq("II", buf);

# test can_borrow_i_from_v
    char buf[6] = "V";

    rnc_borrow(buf, sizeof(buf), 'I');

    ck_assert_str_eq("IIIII", buf);
